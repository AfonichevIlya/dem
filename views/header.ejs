<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">AALTO</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarColor02"
      aria-controls="navbarColor02"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="/catalog">Каталог </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/where">Как нас найти?</a>
        </li>
        <% if (usercheck == 'admin') {%>
        <div class="col"><a href="/admin">Панель администратора</a></div>
        <% }%>
      </ul>
      <% if (usercheck == 'guest') {%>

      <div class="me-2">
        <a href="/auth">
          <button type="button" class="btn btn-outline-light">Войти</button></a
        >
      </div>
      <% }else {%>
      <div class="me-2">
        <a href="/profile" class="btn btn-outline-light"> Профиль </a>
      </div>
      <form action="/auth/logout" method="post" id="myForm">
        <div>
          <button type="submit" id="post-btn" class="btn btn-danger">
            Выйти
          </button>
        </div>
      </form>
      <% }%>
    </div>
  </div>
</nav>
